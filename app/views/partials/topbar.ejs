<header class="topbar">
  <a class="iconbtn" href="<%= typeof back !== 'undefined' && back ? back : 'javascript:history.back()' %>"><%- H.ic('back', 22) %></a>
  <h1><%= heading %></h1>
  <span class="right"><%- typeof right !== 'undefined' ? right : '' %></span>
</header>
